{
  "name": "ai-test-agent",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "demo:seed": "node scripts/seed-demo.mjs",
    "demo:e2e": "node scripts/run-demo-e2e.mjs"
  }
}
